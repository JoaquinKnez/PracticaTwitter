window.addEventListener('load', function() {
    let loading = document.querySelector('#splash-contenedor');

    loading.style.display = 'none' /* queremos que cuando termine de cargar la pagina, no se muestre el gif */
})